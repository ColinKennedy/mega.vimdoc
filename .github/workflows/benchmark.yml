name: Profile Repository
on:
  push:
    branches:
      - main
      # TODO: Remove this later
      - add_profiling

jobs:
  benchmarks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Profile Repository
      - uses: ./.github/actions/neovim-busted-profiler-gh-action

